# Learn with Saleh v55

- Letter and dot outlines for خ، د، ذ، ر، ز، س come from the same Arial contours used by the supplied `حروف.pdf` reference.
- Group-two lessons reuse the accepted Khaa lesson structure and ElevenLabs Laloosh voice with `eleven_v3`.
- Group-two prior reviews are scoped to their package and are skippable; Dal starts without a prior review.
- Checkpoint writing contains free writing only, with no guided tracing task.
- Isolated Alif sound recognition accepts the common iOS/Android transcript variants of the same sound.
- The tap effect is one short synchronized sound, and the first-launch greeting uses the approved Arabic shaping.

